/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CatalogInfo } from '../../models/catalog-info';

export interface CatalogGetCatalog$Json$Params {
}

export function catalogGetCatalog$Json(http: HttpClient, rootUrl: string, params?: CatalogGetCatalog$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<CatalogInfo>> {
  const rb = new RequestBuilder(rootUrl, catalogGetCatalog$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CatalogInfo>;
    })
  );
}

catalogGetCatalog$Json.PATH = '/api/catalog';
