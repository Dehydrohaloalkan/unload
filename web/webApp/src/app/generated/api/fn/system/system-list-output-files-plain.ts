/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OutputFileInfo } from '../../models/output-file-info';

export interface SystemListOutputFiles$Plain$Params {
  path?: string;
}

export function systemListOutputFiles$Plain(http: HttpClient, rootUrl: string, params?: SystemListOutputFiles$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OutputFileInfo>>> {
  const rb = new RequestBuilder(rootUrl, systemListOutputFiles$Plain.PATH, 'get');
  if (params) {
    rb.query('path', params.path, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OutputFileInfo>>;
    })
  );
}

systemListOutputFiles$Plain.PATH = '/api/system/output-files';
