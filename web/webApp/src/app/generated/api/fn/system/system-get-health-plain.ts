/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SystemHealthResponse } from '../../models/system-health-response';

export interface SystemGetHealth$Plain$Params {
}

export function systemGetHealth$Plain(http: HttpClient, rootUrl: string, params?: SystemGetHealth$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<SystemHealthResponse>> {
  const rb = new RequestBuilder(rootUrl, systemGetHealth$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SystemHealthResponse>;
    })
  );
}

systemGetHealth$Plain.PATH = '/api/system/health';
