/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GatewayUploadResponse } from '../../models/gateway-upload-response';
import { IFormFile } from '../../models/i-form-file';

export interface SystemUploadFilesToGateway$Plain$Params {
      body: {
'files'?: Array<IFormFile>;
} & {
'memberName'?: string;
} & {
}
}

export function systemUploadFilesToGateway$Plain(http: HttpClient, rootUrl: string, params: SystemUploadFilesToGateway$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<GatewayUploadResponse>> {
  const rb = new RequestBuilder(rootUrl, systemUploadFilesToGateway$Plain.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/x-www-form-urlencoded');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GatewayUploadResponse>;
    })
  );
}

systemUploadFilesToGateway$Plain.PATH = '/api/system/gateway-upload';
