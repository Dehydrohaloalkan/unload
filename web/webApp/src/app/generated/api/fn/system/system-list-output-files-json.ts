/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OutputFileInfo } from '../../models/output-file-info';

export interface SystemListOutputFiles$Json$Params {
  path?: string;
}

export function systemListOutputFiles$Json(http: HttpClient, rootUrl: string, params?: SystemListOutputFiles$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OutputFileInfo>>> {
  const rb = new RequestBuilder(rootUrl, systemListOutputFiles$Json.PATH, 'get');
  if (params) {
    rb.query('path', params.path, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OutputFileInfo>>;
    })
  );
}

systemListOutputFiles$Json.PATH = '/api/system/output-files';
