/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SystemHealthResponse } from '../../models/system-health-response';

export interface SystemGetHealth$Json$Params {
}

export function systemGetHealth$Json(http: HttpClient, rootUrl: string, params?: SystemGetHealth$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<SystemHealthResponse>> {
  const rb = new RequestBuilder(rootUrl, systemGetHealth$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SystemHealthResponse>;
    })
  );
}

systemGetHealth$Json.PATH = '/api/system/health';
