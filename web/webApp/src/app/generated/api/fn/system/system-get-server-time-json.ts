/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ServerTimeResponse } from '../../models/server-time-response';

export interface SystemGetServerTime$Json$Params {
}

export function systemGetServerTime$Json(http: HttpClient, rootUrl: string, params?: SystemGetServerTime$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ServerTimeResponse>> {
  const rb = new RequestBuilder(rootUrl, systemGetServerTime$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ServerTimeResponse>;
    })
  );
}

systemGetServerTime$Json.PATH = '/api/system/time';
